
/**
 * Configuration for display of a specific advertisement category in the marketplace
 */
export interface AdCategoryConfiguration {

  /**
   * A custom icon for the category. If unset will use the default icon.
   */
  icon?: string;

  /**
   * A custom color for the category. If unset, will use the default color.
   */
  color?: string;

}
