/**
 * The distinct entry point scopes for running operations
 */
export enum OperationRunScope {
  User = 'user',
  Ad = 'ad',
  Record = 'record',
  Transfer = 'transfer',
  Standalone = 'standalone',
}
