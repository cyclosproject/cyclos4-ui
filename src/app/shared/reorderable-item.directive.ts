import { Directive } from '@angular/core';

/**
 * Defines a reorderable element inside a reorderable list
 */
@Directive({
  selector: '[reorderableItem]'
})
export class ReorderableItemDirective {
}
