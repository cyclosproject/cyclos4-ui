import { Directive } from '@angular/core';

/**
 * Defines an element to be the placeholder for drop targets
 */
@Directive({
  selector: '[reorderablePlaceholder]'
})
export class ReorderablePlaceholderDirective {
}
