import { Directive } from '@angular/core';

/**
 * Defines the info window shown when the user clicks a map marker on results
 */
@Directive({
  selector: '[resultInfoWindow]',
})
export class ResultInfoWindowDirective {
}
