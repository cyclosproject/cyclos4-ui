click saved value "newUserName"
click "Block / Disable / Remove user"
check that page contains "Active" on the right of "Current Status"
click "Select the new status..."
click "Purged (all private data is erased)" below the "Removed (no private data is erased)"
enter "testing" into "Comment"
click "Save"
click "Ok"
check that page contains "The user status has been changed"
check that page contains "Purged (all private data is erased)" on the right of "Current Status"
